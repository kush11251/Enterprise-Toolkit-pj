# Enterprise Toolkit
This is a modular toolkit with a main runner script that sources utility functions from a separate lib/ directory.

## Usage
1. Build and run the application using `docker-compose up`.